name: Build macOS app

on:
  workflow_dispatch:
  push:
    branches: [claude/goal-setting-sjhs9v]
    paths:
      - "src/**"
      - "assets/**"
      - "project.godot"
      - "export_presets.cfg"
      - ".github/workflows/build-macos.yml"

# 4.5+ is required for the export: Godot 4.3/4.4 have a macOS regression
# where generic HID joysticks (e.g. the Thrustmaster TCA set) are not
# detected at all (godotengine/godot#102927, fixed in the 4.5 milestone).
env:
  GODOT_VERSION: 4.7-stable
  GODOT_RELEASE: "4.7.stable"

jobs:
  export-macos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache Godot + export templates
        id: cache-godot
        uses: actions/cache@v4
        with:
          path: |
            ~/godot-bin
            ~/.local/share/godot/export_templates
          key: godot-${{ env.GODOT_VERSION }}-macos-template

      - name: Download Godot and macOS export template
        if: steps.cache-godot.outputs.cache-hit != 'true'
        run: |
          set -e
          mkdir -p ~/godot-bin
          curl -sSL -o /tmp/godot.zip \
            "https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}/Godot_v${GODOT_VERSION}_linux.x86_64.zip"
          unzip -q /tmp/godot.zip -d ~/godot-bin
          curl -sSL -o /tmp/templates.tpz \
            "https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}/Godot_v${GODOT_VERSION}_export_templates.tpz"
          mkdir -p ~/.local/share/godot/export_templates/${GODOT_RELEASE}
          unzip -q -j /tmp/templates.tpz templates/macos.zip \
            -d ~/.local/share/godot/export_templates/${GODOT_RELEASE}

      - name: Import project resources
        run: |
          ~/godot-bin/Godot_v${GODOT_VERSION}_linux.x86_64 --headless --path . --import 2>&1 | tail -5 || true

      - name: Headless smoke test (script compatibility gate)
        run: |
          out=$(timeout 120 ~/godot-bin/Godot_v${GODOT_VERSION}_linux.x86_64 --headless --path . --quit-after 240 2>&1 || true)
          echo "$out" | tail -20
          if echo "$out" | grep -qE "SCRIPT ERROR|Parse Error"; then
            echo "::error::GDScript errors under Godot ${GODOT_VERSION}"
            exit 1
          fi

      - name: Export macOS app (universal, ad-hoc signed)
        run: |
          set -o pipefail
          mkdir -p dist
          ~/godot-bin/Godot_v${GODOT_VERSION}_linux.x86_64 --headless --path . \
            --export-release "macOS" dist/C172-FlightSim-macOS.zip 2>&1 | tee /tmp/export.log
          test -s dist/C172-FlightSim-macOS.zip
          ls -la dist/

      - name: Upload app artifact
        uses: actions/upload-artifact@v4
        with:
          name: C172-FlightSim-macOS
          path: dist/C172-FlightSim-macOS.zip
          if-no-files-found: error
